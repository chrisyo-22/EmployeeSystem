<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chrisyo.mapper.ClassMapper">

    <select id="list" resultType="com.chrisyo.entity.Class">
        select c.id,
               c.name,
               c.room,
               c.begin_date,
               c.end_date,
               c.master_id,
               c.subject,
               e.name master_Name,
               c.create_time,
               c.update_time
        from class c left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end == null">
                and c.begin_date = #{begin}
            </if>
            <if test="end != null and begin == null">
                and c.end_date = #{end}
            </if>
            <if test="begin != null and end != null">
                and c.begin_date between #{begin} and #{end} or c.end_date between #{begin} and #{end}
            </if>
        </where>
        order by e.update_time desc
    </select>


    <insert id="add">
        insert into class values (null, #{name}, #{room}, #{beginDate}, #{endDate}, #{masterId}, #{subject}, #{createTime}, #{updateTime})
    </insert>

    <select id="getClassById" resultType="com.chrisyo.entity.Class">
        select c.id,
               c.name,
               c.room,
               c.begin_date,
               c.end_date,
               c.master_id,
               c.subject,
               e.name master_Name,
               c.create_time,
               c.update_time
        from class c left join emp e on c.master_id = e.id
    </select>

    <update id="update">
        update class
        <set>
            name = #{name},
            room = #{room},
            begin_date = #{beginDate},
            end_date = #{endDate},
            master_id = #{masterId}
            <if test="subject != null and subject !=''">
                ,subject = #{subject}
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>